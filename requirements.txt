# Versions pinned to what the reported results were actually produced with.
# Python 3.13.7 on Windows 11, RTX 3060 (6GB).

# PyTorch (CUDA 12.6 build). The extra index is required for the +cu126 wheels,
# note cu121 wheels do not exist for this torch/Python combination.
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.13.0+cu126
torchvision==0.28.0+cu126

# CLIP / model backbones
open_clip_torch==3.3.0
transformers==5.14.1

# data & training
pandas==3.0.5
numpy==2.5.1
scikit-learn==1.9.0
pillow==12.3.0
tqdm==4.70.0
pyyaml==6.0.3

# notebooks
jupyter==1.1.1
ipykernel==7.3.0
matplotlib==3.11.1

# analysis
scipy==1.18.0

# demo app (app.py)
gradio==6.22.0
