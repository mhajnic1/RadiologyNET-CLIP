# patient data, never commit this
/data/

# keeping every notebook out of the repo for now. some of them show real diagnosis
# text or images and data/ alone doesn't cover their saved outputs, so this is the
# blanket version rather than listing them one by one. notebooks/util.py stays.
notebooks/*.ipynb

personalnotes.txt
.claude/
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
checkpoints/**/*.pt
checkpoints/**/*.pth
results/logs/*
results/figures/*
!results/logs/.gitkeep
!results/figures/.gitkeep
